<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".STARX24App"
        android:allowBackup="true"
        android:icon="@drawable/ic_arenax"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArenaX">
        <!-- Firebase is initialized lazily by FirebaseRepository, not in the provider phase,
             so a bad google-services config can never crash app startup. -->
        <provider
            android:name="com.google.firebase.provider.FirebaseInitProvider"
            android:authorities="${applicationId}.firebaseinitprovider"
            tools:node="remove" />
        <activity android:name=".CrashActivity" android:exported="false" />
        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".MessageLoginActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".TournamentControlCenterActivity" />
        <activity android:name=".CategoryMatchesActivity" />
        <activity android:name=".TournamentDetailActivity" />
        <activity android:name=".NotificationsActivity" />
        <activity android:name=".NoticeDetailActivity" />
        <activity android:name=".SupportActivity" />
        <activity android:name=".TeamActivity" />
        <activity android:name=".FaqActivity" />
        <activity android:name=".MyMatchesActivity" />
        <activity android:name=".WalletActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" android:exported="false" />
        <service android:name=".StarxFirebaseMessagingService" android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>
