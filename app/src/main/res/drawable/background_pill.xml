<?xml version="1.0" encoding="utf-8"?>
<!-- Deposit / Winning pill: a distinctly darker red than the balance card behind it,
     so the two don't blend into each other. Still flat, no gradient. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/wallet_flat_red_pill" />
    <corners android:radius="20dp" />
    <stroke android:width="1dp" android:color="@color/blood_red" />
</shape>
